rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /sistema/{userId}/{document=**} {
      allow read, write: if request.auth != null;
    }
    match /sistema_global/{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}